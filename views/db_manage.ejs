<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Database Management - Spirit of Youth Phoenix</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-blue-50 text-gray-800 leading-relaxed">

    <% // Include the header partial %>
    <%- include('_header') %>

    <main class="container mx-auto mt-8 p-4 md:px-8">
        <% // Background element for the logo %>
        <div class="main-content-background"></div>

        <section class="bg-white rounded-lg shadow-xl p-6 mb-8 border-t-4 border-blue-600">
            <h2 class="text-3xl font-bold mb-6 text-blue-800">Database Management</h2>

            <p class="text-lg text-gray-700 mb-6">Manage users and videos directly from this interface.</p>

            <% // Display success or error messages %>
            <% if (message) { %>
                <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
                    <span class="block sm:inline"><%= message %></span>
                </div>
            <% } %>
            <% if (error) { %>
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
                    <span class="block sm:inline"><%= error %></span>
                </div>
            <% } %>

            <% // Users Table %>
            <div class="mb-8">
                <h3 class="text-2xl font-semibold mb-4 text-blue-700">Users</h3>
                <div class="overflow-x-auto">
                    <table class="min-w-full bg-white rounded-lg shadow overflow-hidden">
                        <thead class="bg-blue-100">
                            <tr>
                                <th class="py-2 px-4 text-left text-sm font-semibold text-blue-800">ID</th>
                                <th class="py-2 px-4 text-left text-sm font-semibold text-blue-800">Username</th>
                                <th class="py-2 px-4 text-left text-sm font-semibold text-blue-800">Role</th>
                                <th class="py-2 px-4 text-left text-sm font-semibold text-blue-800">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(function(user) { %>
                                <tr class="border-b border-gray-200 hover:bg-gray-50">
                                    <td class="py-2 px-4 text-sm text-gray-700"><%= user.id %></td>
                                    <td class="py-2 px-4 text-sm text-gray-700"><%= user.username %></td>
                                    <td class="py-2 px-4 text-sm text-gray-700">
                                        <% // Role Display and Edit Form %>
                                        <form action="/coach-area/users/<%= user.id %>/update-role" method="POST" class="inline-flex items-center">
                                            <select name="role" class="form-select mt-1 block w-full rounded-md border-gray-300 shadow-sm text-sm focus:border-blue-300 focus:ring focus:ring-blue-200 focus:ring-opacity-50">
                                                <option value="parent" <%= user.role === 'parent' ? 'selected' : '' %>>Parent</option>
                                                <option value="coach" <%= user.role === 'coach' ? 'selected' : '' %>>Coach</option>
                                            </select>
                                            <button type="submit" class="ml-2 bg-blue-500 hover:bg-blue-700 text-white text-xs font-bold py-1 px-2 rounded">Update</button>
                                        </form>
                                    </td>
                                    <td class="py-2 px-4 text-sm text-gray-700">
                                        <% // Delete Form %>
                                        <form action="/coach-area/users/<%= user.id %>/delete" method="POST" class="inline-block" onsubmit="return confirm('Are you sure you want to delete user <%= user.username %>?');">
                                            <button type="submit" class="bg-red-500 hover:bg-red-700 text-white text-xs font-bold py-1 px-2 rounded">Delete</button>
                                        </form>
                                    </td>
                                </tr>
                            <% }); %>
                            <% if (users.length === 0) { %>
                                <tr>
                                    <td colspan="4" class="py-4 text-center text-gray-600">No users found.</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

            <% // Videos Table (Display only for now) %>
            <div>
                <h3 class="text-2xl font-semibold mb-4 text-blue-700">Videos</h3>
                <div class="overflow-x-auto">
                    <table class="min-w-full bg-white rounded-lg shadow overflow-hidden">
                        <thead class="bg-blue-100">
                            <tr>
                                <th class="py-2 px-4 text-left text-sm font-semibold text-blue-800">ID</th>
                                <th class="py-2 px-4 text-left text-sm font-semibold text-blue-800">Title</th>
                                <th class="py-2 px-4 text-left text-sm font-semibold text-blue-800">Price</th>
                                <% // Add header for actions (e.g., Edit, Delete) - Add forms later %>
                                <th class="py-2 px-4 text-left text-sm font-semibold text-blue-800">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% videos.forEach(function(video) { %>
                                <tr class="border-b border-gray-200 hover:bg-gray-50">
                                    <td class="py-2 px-4 text-sm text-gray-700"><%= video.id %></td>
                                    <td class="py-2 px-4 text-sm text-gray-700"><%= video.title %></td>
                                    <td class="py-2 px-4 text-sm text-gray-700">
                                        £<%= typeof video.price === 'number' ? video.price.toFixed(2) : '0.00' %>
                                    </td>
                                    <td class="py-2 px-4 text-sm text-gray-700">
                                        <% // Add action links/buttons here %>
                                        <% // Example: <a href="/coach-area/videos/<%= video.id %>/edit" class="text-blue-600 hover:underline mr-2">Edit</a> %>
                                        <% // Example: <a href="/coach-area/videos/<%= video.id %>/delete" class="text-red-600 hover:underline">Delete</a> %>
                                    </td>
                                </tr>
                            <% }); %>
                            <% if (videos.length === 0) { %>
                                <tr>
                                    <td colspan="4" class="py-4 text-center text-gray-600">No videos found.</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

            <% // Add sections for adding new users or videos here later %>
            <% // Example: %>
            <% // <div class="mt-8"> %>
            <% //    <h3 class="text-2xl font-semibold mb-4 text-blue-700">Add New User</h3> %>
            <% //    <% // Add a form for adding users here %> %>
            <% // </div> %>

        </section>
    </main>

    <footer class="bg-gray-900 text-gray-300 p-6 mt-12">
        <div class="container mx-auto text-center text-sm">
            &copy; <%= new Date().getFullYear() %> Spirit of Youth Phoenix. All rights reserved.
            <div class="mt-2">
                <a href="#" class="text-gray-400 hover:text-white mx-2">Privacy Policy</a>
                <span class="text-gray-600">|</span>
                <a href="#" class="text-gray-400 hover:text-white mx-2">Terms of Service</a>
            </div>
        </div>
    </footer>
</body>
</html>
